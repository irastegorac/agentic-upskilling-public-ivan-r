Java version (e.g., Java 17)
- Build and test commands
- The requirement to always run tests after making changes
- Any non-obvious rules to avoid (e.g., do not use field injection with `@Autowired`, do not introduce new dependencies)
-  Always use constructor injection